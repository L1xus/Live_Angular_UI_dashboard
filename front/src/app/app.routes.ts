import { Routes } from '@angular/router';
import { DashboardComponent } from './dash/dash.component';

export const routes: Routes = [
  { path: '', component: DashboardComponent } // Set dashboard as the default route
];
